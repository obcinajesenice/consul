<% if feature?("community") %>
  <h3><%= t("community.sidebar.title") %></h3>
  <p>
    <%= community_access_text(community) %>
  </p>
  <%= link_to t("community.sidebar.button_to_access"), community_path(community.id), class: 'action-button small' %>
<% end %>
